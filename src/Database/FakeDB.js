import * as __ from '../Util/ParamCheck';
import * as DOM from '../DOM/Elements';

// this module mocks/provides the connection to a databae



let store = {};

const getStorage = () => {
    return store;
};

const Locker = () => {
    this.id = DOM.genId();

    locker.write = (propName, propValue) => {

    }
    locker.read = (propName) => {

    }

    store[this.id] = this;
}

Locker.prototype.write = (propName, propValue) => {

}


const createTable = function () {

}